{
  "name": "kiseis-playlist",
  "version": "1.0.0",
  "private": true,
  "description": "Static playlist site for Cloudflare Pages with a minimal Pages Function proxy.",
  "scripts": {
    "dev": "wrangler pages dev",
    "deploy": "wrangler pages deploy",
    "deploy:preview": "wrangler pages deploy --branch preview"
  },
  "devDependencies": {
    "wrangler": "^4.0.0"
  }
}
